package il.ac.technion.cs.sd.grades.ext;

public class LineStorage {
    /** Appends a line to the end of the file. */
    public static void appendLine(String s) {}

    /**
     * Returns the contents of the string that was stored in the input line number.
     * @throws IndexOutOfBoundsException If the line number is negative or if the index was too large.
     */
    public static String read(int lineNumber) throws InterruptedException { return ""; }

    /** Returns the total number of lines in the file. */
    public static int numberOfLines() throws InterruptedException { return 0; }
}

